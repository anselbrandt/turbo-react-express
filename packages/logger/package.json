{
  "name": "logger",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup",
    "lint": "eslint src/",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "watch": "tsup --watch"
  },
  "jest": {
    "preset": "jest-presets/jest/node"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "eslint-config-custom": "*",
    "jest-presets": "*",
    "tsconfig": "*",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  }
}
